{
    function_array = [
        fn(a) { a },
        fn(a) { a * 2 },
        fn(a) { a * a },
    ];

    for[] (i,0..#function_array) {
        function_array[i](5)
    }
}

//program should output [5, 10, 25] with an empty environment
