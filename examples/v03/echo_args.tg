// echo_args — print each command-line argument on its own line.
//
// usage: tigr echo_args.tg foo bar 'baz qux'

Os := import 'Os';

// Skip args[0] (the interpreter) and args[1] (this script).
for (i, a, Os.args) {
    if i >= 2 { print(a) }
}
