// Naive recursive Fibonacci — no memoisation, no tail calls.
// Exercises call-frame setup/teardown, comparisons, and addition.

fib := fn(n) {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
};

fib(28)
